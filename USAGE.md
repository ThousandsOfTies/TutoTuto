# TutoTutoの使い方

## 公開アプリ

[アプリを開く](https://thousandsofties.github.io/TutoTuto/)。
公開アプリの利用者がGemini APIキーを設定する必要はない。キー設定はAPIサーバーの開発・運用者向け。

1. 教材一覧からPDFまたは画像を取り込む。画像はPDFに変換される。
2. 教材を開き、ページ移動・ズームで問題を表示する。
3. ペンやテキストで解答を書き込む。消しゴムやUndoで修正する。
4. 問題文と解答を含む範囲を選び、AI採点を実行する。
5. 正誤・正解・解説を確認する。履歴は管理画面から参照できる。
6. SNSリンクを設定している場合は、採点後の結果画面から利用できる。

PDF・書き込み・採点履歴はブラウザの `TutoTutoDB` に保存される。
ブラウザのサイトデータ削除やアプリのデータ削除操作で失われる。
AI採点にはネットワーク接続が必要。

## ローカルで開発する場合

セットアップは [README](README.md#ローカル開発) を参照。
APIは `repos/tutotuto-app/.env` を読む。フロント設定は同ディレクトリの `.env.local` を使用する。

```bash
# メタリポジトリ内、別々のターミナルで実行
make dev
make dev-server
```

または `repos/tutotuto-app` 内で `npm run dev:all` を実行する。
フロントは既定3000、APIは3003。ログは各起動ターミナルに出る。

採点時の接続エラーはAPIの起動状態と `VITE_API_URL` を確認する。
ベースURLは `http://localhost:3003` とし、`/api` を付けない。
