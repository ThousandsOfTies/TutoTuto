# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build artifacts (generated in repos/)
node_modules
dist
dist-ssr
deploy
*.local

# Package lock files (managed in each repo)
package-lock.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (managed in each repo)
.env
.env.local
.env.production
.env.*

# PDF files (managed in repos/)
PDFs/

# Submodules are tracked by Git; do not ignore repos/ entries here.
